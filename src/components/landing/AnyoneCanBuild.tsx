import Image from "next/image";
import { Reveal } from "@/components/motion/Reveal";

type Part = {
  src: string;
  alt: string;
  prompt: string;
  span: string;
  aspect: string;
};

const PARTS: Part[] = [
  {
    src: "/parts/valve-housing.png",
    alt: "A three-port valve housing with a threaded bore, generated by Archie and rendered from its STEP output.",
    prompt: "a three-port valve housing with a threaded bore",
    span: "lg:col-span-8",
    aspect: "aspect-[16/10]",
  },
  {
    src: "/parts/gearbox-housing.png",
    alt: "A ribbed gearbox housing with an oval mounting flange, generated by Archie.",
    prompt: "a ribbed gearbox housing, oval flange",
    span: "lg:col-span-4",
    aspect: "aspect-[16/10]",
  },
  {
    src: "/parts/bell-housing.png",
    alt: "A bell housing with bolt bosses and gusseted webs, generated by Archie.",
    prompt: "a bell housing with bolt bosses and gussets",
    span: "lg:col-span-4",
    aspect: "aspect-[4/3]",
  },
  {
    src: "/parts/sensor-mount.png",
    alt: "A sensor mount with two mounting tabs and an inset circular pocket, generated by Archie.",
    prompt: "a sensor mount, two tabs, inset pocket",
    span: "lg:col-span-4",
    aspect: "aspect-[4/3]",
  },
  {
    src: "/parts/volute-plate.png",
    alt: "A pump volute backplate with a central boss, generated by Archie.",
    prompt: "a pump volute backplate",
    span: "lg:col-span-4",
    aspect: "aspect-[4/3]",
  },
  {
    src: "/parts/intake-gasket.png",
    alt: "A four-runner intake gasket, generated by Archie.",
    prompt: "a four-runner intake gasket",
    span: "lg:col-span-12",
    aspect: "aspect-[24/6]",
  },
];

function PartTile({ part }: { part: Part }) {
  return (
    <figure
      className={`part-lightbox group relative overflow-hidden rounded-2xl ${part.span} ${part.aspect}`}
    >
      <Image
        src={part.src}
        alt={part.alt}
        fill
        sizes="(max-width: 1024px) 100vw, 55vw"
        className="object-contain p-4 transition-transform duration-700 ease-[cubic-bezier(0.22,1,0.36,1)] group-hover:scale-[1.03] md:p-5"
      />
      <span className="absolute left-4 top-4 font-mono text-[10px] uppercase tracking-[0.16em] text-[#4a5675]">
        Archie · built
      </span>
      <figcaption className="absolute inset-x-0 bottom-0 flex items-center gap-2 bg-gradient-to-t from-black/[0.06] to-transparent px-4 pb-4 pt-8">
        <code className="u-cipher text-[12px] text-[#1a2236] md:text-[12.5px]">
          <span className="text-[#4a5675]">you:</span> {part.prompt}
        </code>
      </figcaption>
    </figure>
  );
}

export function AnyoneCanBuild() {
  return (
    <section id="output" className="relative scroll-mt-24 py-24 md:py-32">
      <div className="mx-auto w-full max-w-[1400px] px-5 md:px-10">
        <div className="max-w-3xl">
          <Reveal>
            <p className="eyebrow">Real output · not renders</p>
          </Reveal>
          <Reveal delay={0.06}>
            <h2 className="mt-6 font-display text-h1 text-ink text-balance">
              If you can describe it, you can build it.
            </h2>
          </Reveal>
          <Reveal delay={0.12}>
            <p className="mt-6 max-w-[56ch] text-lead text-ink-soft text-pretty">
              Every part below was generated by Archie from a single plain-language line —
              then rendered straight from the real STEP geometry it produced. No modeling,
              no CAD degree, no cloud.
            </p>
          </Reveal>
        </div>

        <Reveal delay={0.08}>
          <div className="mt-12 grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-12 lg:gap-5">
            {PARTS.map((p) => (
              <PartTile key={p.src} part={p} />
            ))}
          </div>
        </Reveal>
      </div>
    </section>
  );
}
